node_modules/
dist/
*.log
.env
.env.*
.DS_Store

# Local design drafts (specs, plans) — kept local, not versioned
docs/superpowers/

# Local spikes (throwaway investigation) — kept local, not versioned
spikes/
tests/fixtures/
